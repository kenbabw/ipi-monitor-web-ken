/**
 * Application Version
 * Update this version number with each new build/release
 * 
 * Format: X.Y
 * - X = Major version (breaking changes, major features)
 * - Y = Minor version (bug fixes, small features)
 * 
 * Version History:
 * - 1.0 (2025-10-16): Initial release with chart functionality, device info, password reset
 */
export const APP_VERSION = '1.1';
